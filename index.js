const lista = require('./tarefa1/usuarios-api/usuarios-api.json');

//Responsável por criar a função de listar todos os usuários acima de 18 anos.

const listarMaiores = () => {
    return usuarios-api.filter(usuarios.idade) > 18
}
console.log(listarMaiores)